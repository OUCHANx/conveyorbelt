using UnityEngine;

public class ZukanManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    //図鑑にあるパンを押すとパンの説明を見ることができる
}
